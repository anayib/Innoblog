<h1>New Article</h1>

<%= form_for :article, url: articles_path do |f| %> 
<!-- se especifica la url:articles_path para que 
	mande los parámetros al controlador articles 
	pero que no tome la acción new sino que asocie
	el envio con un post para que se de el create	
 -->
  <% if @article.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@article.errors.count, "error") %> prohibited
      this article from being saved:</h2>
      <!-- el pluralize permite pluralizar el mensaje según el número de herrores que vienen en el array -->

    <ul>
     <!-- esto se podría hacer si sólo quiero mostrar un error a la vez, y los mando con un alert# <% @article.errors.full_messages[0] %> -->
    
    <% @article.errors.full_messages.each do |msg| %>   

      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>


  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>

